# Client (React + Vite)

See the [root README](../README.md) for full setup instructions
(Google OAuth credentials, environment variables, and how to run both
the client and server together).

Quick start (server must be running separately on port 4000):

```bash
npm install
npm run dev
```
